angular.module("<%= moduleName %>"<% if (deps) { %>, <%- JSON.stringify(deps) %><% } %>)
<% constants.forEach(function(constant) { %>
.constant("<%= constant.name %>", <%- JSON.stringify(constant.value, null, space) %>)
<% }) %>
;